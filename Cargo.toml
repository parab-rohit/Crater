[package]
name = "Crater"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.29", features = ["sched", "mount", "process", "user", "hostname", "fs", "ioctl", "term"] }
#loopdev = "0.4.0"
libc = "0.2.180"